# 状态摘要示例

## 输出格式

程序会**每隔 15 秒**（3 次检测周期）自动打印所有任务的状态摘要：

```
2026-02-03 21:30:45 [INFO] ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-02-03 21:30:45 [INFO] 📋 [状态摘要] 当前任务状态:
2026-02-03 21:30:45 [INFO]   ▶ 1. task-debug-feature        | ● running   | abc12345
2026-02-03 21:30:45 [INFO]     2. task-review-code          | ● completed | def67890
2026-02-03 21:30:45 [INFO]     3. task-write-tests          | ○ pending   | ghi23456
2026-02-03 21:30:45 [INFO] ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 字段说明

| 列 | 说明 |
|----|------|
| `▶` | 当前激活的任务（只有一个） |
| ` ` | 非激活任务 |
| 序号 | 任务编号（1, 2, 3...） |
| 任务名 | 最多显示前 30 个字符 |
| 状态符号 | `○` pending / `●` running / `●` completed |
| 任务ID | 显示前 8 个字符 |

## 状态含义

| 状态 | 符号 | 说明 |
|------|------|------|
| `pending` | ○ | 任务已创建，等待用户输入 |
| `running` | ● | Claude 正在工作 |
| `completed` | ● | Claude 已完成，等待查看 |

## 查看方法

### 方法 1: 使用 watch_logs.sh（推荐）

```bash
./watch_logs.sh
```

状态摘要会以**蓝色加粗**显示，非常显眼。

### 方法 2: 直接查看日志

```bash
# 查看最近 3 次状态摘要
grep "\[状态摘要\]" ~/.config/claude-manager/logs/app.log | tail -3

# 实时查看状态摘要
tail -f ~/.config/claude-manager/logs/app.log | grep "\[状态摘要\]"
```

## 调整打印频率

在 `src/claude_manager/app.py` 中修改：

```python
# 当前: 每3次检测打印一次（15秒）
if self._status_print_counter >= 3:

# 改为: 每2次检测打印一次（10秒）
if self._status_print_counter >= 2:

# 改为: 每次检测都打印（5秒）
if self._status_print_counter >= 1:
```

## 示例场景

### 场景 1: 刚启动，有3个任务

```
📋 [状态摘要] 当前任务状态:
  ▶ 1. implement-auth-feature     | ○ pending   | a1b2c3d4
    2. fix-db-connection          | ● completed | e5f6g7h8
    3. refactor-api-layer         | ○ pending   | i9j0k1l2
```

### 场景 2: 正在工作中

```
📋 [状态摘要] 当前任务状态:
  ▶ 1. implement-auth-feature     | ● running   | a1b2c3d4
    2. fix-db-connection          | ● completed | e5f6g7h8
    3. refactor-api-layer         | ○ pending   | i9j0k1l2
```

### 场景 3: 第一个任务完成

```
📋 [状态摘要] 当前任务状态:
  ▶ 1. implement-auth-feature     | ● completed | a1b2c3d4
    2. fix-db-connection          | ● completed | e5f6g7h8
    3. refactor-api-layer         | ○ pending   | i9j0k1l2
```

## 与状态检测的配合

状态摘要会与详细的状态检测日志交替出现：

```
# 详细检测（每5秒）
[状态检测] 任务 a1b2c3d4, 当前状态: running
[Kitty标志] at_prompt=False (window_id=148)
[活动监控] has_activity=True, is_silent=False
[内容分析] line_count=45, is_welcome=False

# 状态摘要（每15秒）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 [状态摘要] 当前任务状态:
  ▶ 1. implement-auth-feature     | ● running   | a1b2c3d4
    2. fix-db-connection          | ● completed | e5f6g7h8
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 继续详细检测...
```
